const config = {
  appId: "com.finanzasia.app",
  appName: "FinanzasIA",
  webDir: "public", // 👈 déjalo así para evitar errores
  server: {
    url: "https://finanzas-ia-six.vercel.app",
    cleartext: true
  }
};

export default config;